---
const points = [
  {
    title: "רקע מאופנה",
    text: "לפני אתרים עבדתי בעיצוב אופנה. משם למדתי לשים לב לפרטים, לצבע ולמראה.",
  },
  {
    title: "עסקים מתחילים",
    text: "אני עובד הרבה עם עסקים שמתחילים: מלווים, בונים יחד, ומסדרים נוכחות ברשת.",
  },
  {
    title: "מעט פרויקטים",
    text: "אני לוקח מעט פרויקטים ומקדיש לכל אחד תשומת לב.",
  },
  {
    title: "בודק תוכנה",
    text: "אני עובד בחברה ממשלתית כבודק תוכנה, עם ניסיון במערכות. לפני שהאתר עולה אני בודק שהכול עובד.",
  },
  {
    title: "מחירים הוגנים",
    text: "הצעת מחיר ברורה מראש. אתם משלמים על מה שביקשתם.",
  },
];
---

<section class="scene about" id="about">
  <div class="about-inner">
    <p class="eyebrow about-eye">קצת עליי</p>
    <h2>מה חשוב לי שתדעו עליי</h2>
    <div class="points">
      {
        points.map((p, i) => (
          <div class="point">
            <span class="n">{String(i + 1).padStart(2, "0")}</span>
            <div>
              <h3>{p.title}</h3>
              <p>{p.text}</p>
            </div>
          </div>
        ))
      }
    </div>
    <a href="#choose" class="btn btn-primary about-cta">בחרו מה אתם צריכים</a>
  </div>
</section>

<style>
  .scene.about {
    min-height: 100svh;
    display: flex;
    align-items: center;
    background: var(--ink);
    color: var(--paper);
    padding: 90px 0 calc(var(--bar-height) + 40px);
  }

  .about-inner {
    width: 100%;
    max-width: var(--max-width);
    margin: 0 auto;
    padding: 0 20px;
  }

  .about-eye {
    background: var(--accent);
    color: #fff;
  }

  h2 {
    font-size: clamp(2.2rem, 9vw, 3.6rem);
    margin-bottom: 36px;
  }

  .points {
    display: flex;
    flex-direction: column;
    gap: 28px;
  }

  .point {
    display: grid;
    grid-template-columns: 56px 1fr;
    gap: 14px;
    align-items: start;
    padding-bottom: 24px;
    border-bottom: 1px solid rgba(246, 241, 231, 0.18);
  }

  .point:last-child {
    border-bottom: none;
    padding-bottom: 0;
  }

  .n {
    font-family: "Suez One", serif;
    font-size: 1.4rem;
    color: var(--accent);
  }

  h3 {
    font-size: 1.3rem;
    margin-bottom: 6px;
  }

  .point p {
    color: #d2cbc0;
    font-size: 1rem;
    max-width: 42ch;
  }

  .about-cta {
    margin-top: 36px;
    width: 100%;
    max-width: 320px;
  }

  @media (min-width: 768px) {
    .about-inner {
      padding: 0 32px;
    }

    .points {
      display: grid;
      grid-template-columns: 1fr 1fr;
      gap: 36px 48px;
    }

    .point {
      border-bottom: none;
      padding-bottom: 0;
    }

    .about-cta {
      width: auto;
    }
  }
</style>
